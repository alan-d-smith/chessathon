.venv/
# A second environment with CUDA torch, used only to fit the network. The main venv
# stays CPU-only so it matches the competition platform exactly.
.venv-train/
__pycache__/
.mypy_cache/
.ruff_cache/
submission.zip
*.pgn

# stockfish-labelled datasets, regenerated not committed. The opening suite is the exception:
# it is the yardstick every A/B match is measured on, so it has to stay fixed across sessions.
data/*
!data/openings.txt
